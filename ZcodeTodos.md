# noxcode 对齐 ZCode Agent 功能 TODO

核查日期：2026-09-21。依据本地 noxcode 与 `ZCode/apps/zcode-cli` 源码及本次差距核查；尚未进行两边应用的运行对照验收。

目标：在现有 Rust/Tauri 架构上补齐通用 Agent 能力，保留 SSH 工作区、LSP、ApplyPatch、隔离 worktree、SQLiteQuery、Process/Monitor 和本机 Computer。本文记录待实施任务，不代表相关功能已经完成。

## 范围与现有基础

| 项目 | 已有基础 | 本次需要补齐 |
| --- | --- | --- |
| 浏览器 | MCP 设置已有 Playwright 预设 | 可重复安装、技能指导、运行环境、浏览器生命周期及桌面整合 |
| 会话 | transcript 持久化、恢复续聊、整段 fork | 消息级分支、回滚与文件快照关联、多客户端恢复 |
| steer | 内部 followup 通道及模型调用边界注入 | 用户可操作的当前回合转向及竞态处理；保留下一回合排队 |
| 自动化 | 创建、查询、更新、删除后端和设置入口 | Agent 的 CronUpdate 工具 |
| 媒体 | 图片数量/大小限制；压缩摘要已经转为纯文本 | PDF、视频、统一媒体预算、附件持久化与媒体引用保留 |
| 工具契约 | 权限、副作用、并行安全、超时、输出预算 | 输出 schema、运行时校验和模型能力适配 |
| MCP OAuth | 授权码、PKCE、令牌存储与刷新 | 元数据发现、按服务能力注册客户端、授权过程协调 |

## 实施约束与顺序

- 保持 `React → Tauri IPC → Rust service → SQLite`；前端不得直接读写数据库。
- 数据库迁移版本接续实施时的最新版本，保持连续，不在本清单中预占版本号。
- 所有 Git 操作继续经过 `src-tauri/src/git/runner.rs`；russh 保持 ring 后端。
- 保持基础应用仅要求用户预装系统 Git。Node、Python、PDF 渲染器、浏览器等新增能力采用库、随包组件或明确启用的可选扩展；缺少可选依赖不能导致基础应用无法启动。
- 涉及执行环境时明确本机、SSH 远端及浏览器所在主机，不得在远程能力失败时静默回退本机。
- 先实施 P0；随后按依赖推进 P1/P2。P3 的动态工作流和多端 Host 分别作为独立子项目。
- 下列每项都包含完成标准；实现时以这些标准设计针对性测试，并执行文末共同检查。大型任务应在开始实施前继续拆为可单独验收的切片。

## P0：现有链路补齐

主要入口：`src-tauri/src/native/agent/loop.rs`、`model/`、`session.rs`、`input_queue.rs`、`scheduler.rs`、`tools/catalog.rs`、`tools/dispatch.rs`、`tools/web.rs`、`src/components/session/`。

- [x] **Q01：增加 CronUpdate 工具。** 复用 `update_automation`，补工具 schema、权限、分发和结果展示。
  - 依赖：无。
  - 验收：可按 ID 修改名称、提示词、计划、启停、渠道和模型；保留 ID 与既有运行记录；未提供字段保持不变；校验工作区归属和权限，错误计划不落库。
  - 验证：14 项自动化/工具契约相关测试、Clippy、格式检查通过；独立审查通过。

- [x] **Q02：批准后的计划写入工作区。** 保存为 `.noxcode/plans/plan-<session>.md`，在后续回合提供文件引用。
  - 依赖：无。
  - 验收：拒绝的计划不作为已批准计划保存；批准路径可在本地和 SSH 工作区原子写入；续聊能定位计划；写入失败明确展示且不伪报成功。
  - 验证：47 项计划相关 Rust 测试、53 项前端相关测试、构建/lint/Clippy/格式检查及独立复审通过；SSH 为本地 shell 夹具验证，尚未进行真实 SSH/跨平台桌面手工验收。

- [x] **Q03：完善 WebFetch 网络访问策略。** 公网抓取默认限制本地/私网目标，本地开发服务通过显式授权访问。
  - 依赖：无。
  - 验收：覆盖 URL、IPv4/IPv6、DNS 解析目标和重定向链；落实到实际连接目标，避免只做域名字符串检查；缓存不能绕过当前策略；代理、企业内网和 localhost 开发场景有明确规则。
  - 验证：41 项网络相关测试、1 项权限管理器测试、16 项前端相关测试及 Clippy/lint/TypeScript/格式检查通过，独立审查通过；本地 HTTP/TLS/代理夹具覆盖地址固定、授权、缓存、配置变化、NO_PROXY、CA、取消及读取上限。补充真实本地 HTTPS 代理传输与 CA 校验通过；真实企业代理尚未手工验证。

- [x] **Q04：模型响应保留结构化结束原因。** 在 OpenAI、Anthropic、Responses 适配层区分正常结束、输出上限、上下文不足、拒绝和流未正常结束。
  - 依赖：无。
  - 验收：流式和非流式响应都能向 Agent 返回可判断的结束状态；缺失或未知字段有明确降级；半截工具参数不进入执行器。
  - 验证：三协议、严格终止证据、错误分支与整批工具校验回归通过；最终模型相关测试 140 项通过，现有真实渠道测试未配置凭据而保持忽略；独立复审通过。

- [x] **Q05：输出截断自动续写。** 保存有效的部分回答，再追加内部续写指令。
  - 依赖：Q04。
  - 验收：正常回答不误触发；初始策略最多自动续写 3 次，耗尽后明确报告未完成；续写受取消和预算控制，并保持 transcript、流式展示与工具调用一致。
  - 验证：主/子 Agent 的实际 HTTP 夹具及前端回归通过；浏览器使用真实组件和测试 IPC 验证实时/历史片段、代码围栏和精确复制；独立复审通过。

- [x] **Q06：开放用户回合内 steer。** 增加 IPC 和 Composer 操作，区分“转向当前回合”和“排队到下一回合”。
  - 依赖：无；复用现有内部 steer 通道。
  - 验收：携带当前回合标识并校验输入大小、会话权限及附件；在下一安全调用边界进入上下文；返回接收状态；排队功能继续可用，不声称能修改已经发出的模型请求。
  - 验证：文字/图片、持久化去重、输入 Hook、串行/并行安全边界、主/子交互隔离及浏览器入口验证通过；前端传输失败保留 UUID 和草稿，收到确认后才清空，独立复审通过。

- [x] **Q07：处理 steer 与回合结束的竞态。** 统一最终回答、工具批次、取消、计划审批和转向消息的消费顺序。
  - 依赖：Q06。
  - 验收：回合结束前后发送的指令不丢失、不重复、不误投其他回合；已接收但未执行的输入可见；转向不能绕过计划或工具权限。
  - 验证：接收/关闭原子边界、并发重试、批准记录事务回滚、旧实例/旧回合事件、快照乱序、配置响应、退出副作用幂等及重启恢复回归通过，独立复审通过。

阶段验收：用户能在 Agent 工作中转向；长输出触顶后能继续；自动化可由 Agent 修改；批准计划可被下一回合引用。核对重启后 transcript 与展示状态。

本次验证范围：后端使用本地可控 HTTP/TLS/代理、SQLite 和 shell 夹具；浏览器使用真实前端组件及测试 IPC，覆盖排队/转向分离、丢回包重试、主/子授权交互、失败计划恢复、续写展示/复制及中断文字恢复。尚未进行真实 Tauri 桌面与模型渠道、真实 SSH 工作区和 Windows/Linux 的整套手工验收；SSH 计划保存验证为本地 shell 夹具，不能等同真实远程验收。

P0 全量检查：Rust **935 通过、3 项既有真实服务测试忽略**；Vitest **88 个文件、741 项通过**；Clippy（所有 target、警告视为错误）、Rustfmt、lint、生产构建和格式检查通过，迁移版本连续至 13，`aws-lc-rs` 无匹配，整体独立代码审查通过。构建仍有既有的大 chunk 提示。全量测试发现并修正了既有 worktree 测试对 macOS `/var` 与 `/private/var` 的字符串比较，改为校验相同的真实目录，保留创建/列出/删除断言。

## P1：恢复、回滚与任务完成核验

主要入口：`src-tauri/src/native/session.rs`、`transcript.rs`、`protocol.rs`、`agent/loop.rs`、`model/client.rs`、`goals.rs`、`src-tauri/src/git/checkpoint.rs`。

- [x] **R01：建立消息与回合的持久标识和边界。** 将消息、工具调用、提交结果、分支和检查点关联起来，为恢复和回滚提供依据。
  - 依赖：复用 Q06 的回合标识设计，避免再引入不一致的 ID 体系。
  - 验收：重启后关联保持稳定；历史数据可迁移或明确标记能力不足；保留回滚所需历史，不仅保存当前压缩后的上下文。
  - 自动化证据（2026-09-22，未勾选）：迁移 14 与 `native/history.rs` 覆盖压缩后身份不变、保存失败不推进修订号、旧 transcript 能力缺口、源会话删除后引用分支仍在、孤立工具调用不进投影。全库 Rust 单测曾 943 通过，修正迁移描述和 17 张表计数后的 database 测试通过；Clippy `--all-targets -D warnings` 通过。未做真实 Tauri、模型渠道、SSH 与外部编辑器验收。

- [x] **R02：流空闲与异常结束检测。** 区分首包等待、流中空闲、网络中断、协议正常结束和用户取消。
  - 依赖：Q04。
  - 验收：半截内容不会被当作成功完成；可重试错误和终止错误有稳定分类；超时、退避和取消可在前端观察，恢复次数有上限。
  - 自动化证据（2026-09-22）：OpenAI、Anthropic、Responses 的首包挂起、流空闲和取消分类通过；心跳不延长空闲计时；半截流仍是无合法终止；默认 6 次重试到第 7 次请求停止，`[重试]` 行可见；退避期间取消不再发下一次请求。Clippy `--all-targets -D warnings` 通过。未做真实渠道长连接验收。

- [x] **R03：从已提交的安全边界恢复模型调用。** 扩展现有请求重试，记录恢复锚点、尝试次数和工具结果提交状态。
  - 依赖：R01、R02。
  - 验收：重试保留已提交工具结果；丢弃未确认的半截工具调用；不会盲目重放有副作用操作；结果不确定的外部执行明确报告，不宣称普遍的恰好执行一次保证。
  - 自动化证据（2026-09-22）：迁移 15 增加 `native_tool_runs` 与 `native_model_attempt_budgets`，业务表 19 张。已提交结果跨重启复用并保持调用顺序；已开始但未提交的副作用标为「结果未知」且不自动重放；无工具计划时服务端续接 id 不能代替本地锚点；进程重启不重置已用重试次数，成功请求后计数归零。转向中断仍保留已完成工具结果。Clippy `--all-targets -D warnings` 通过。未做真实 Tauri、模型渠道、SSH 与外部编辑器验收。

- [ ] **R04：消息级对话 fork 与 rewind。** 支持选择某条消息分叉或回退，并正确重建对话上下文。
  - 依赖：R01。
  - 验收：消息、工具调用及结果成对保留；计划、目标等关联状态遵循分支边界；源分支可追溯；运行中操作不会与新消息提交互相覆盖。

- [ ] **R05：消息级文件回滚预览与应用。** 将消息边界关联至工作区文件检查点，支持“仅对话 / 仅文件 / 两者”。
  - 依赖：R04。
  - 验收：先展示改动范围与冲突；不覆盖用户后续修改或其他会话的未确认改动；覆盖新增、删除、修改文件及本地/SSH；失败可恢复，并明确不撤销外部 API、消息发送等副作用。

- [ ] **R06：目标完成核验。** 在 Goal 完成流程中增加验收条件、验证结果与未完成时的继续执行策略。
  - 依赖：无；恢复/回滚关联部分在 R01 后接入。
  - 验收：优先使用测试或明确产物证据，可辅以轻量模型复核；验证失败、超时、取消不能静默标为完成；自动继续受预算和次数限制，核验过程可查看。

阶段验收：通过中断流、进程重启、工具执行后断线及并发文件修改场景，核对恢复、分叉、回滚的状态一致性。

## P1：媒体与附件链路

主要入口：`src-tauri/src/native/images.rs`、`model/types.rs`、`model/`、`model_catalog.rs`、`transcript.rs`、`tools/local.rs`、`tools/ssh.rs`、`src/components/session/Composer.tsx`。

- [ ] **M01：确定可选运行组件的交付方式。** 明确 PDF、浏览器、JS 和文档技能所需组件采用库、随包还是按需安装。
  - 依赖：无。
  - 验收：形成 Windows/macOS/Linux 的组件、版本、安装、升级和失败降级清单；无需新增系统级必装依赖即可启动基础应用；可选组件缺失有可执行的提示。

- [ ] **M02：附件持久化与引用。** 将会话媒体存为受管理的附件，transcript 保存引用，替代保存时直接移除图片的现状。
  - 依赖：无；与 R01 的消息关联设计保持一致。
  - 验收：重启、续聊和 fork 后可重新读取附件；丢失引用能诊断；清理不会删除仍被分支使用的文件；跨工作区读取遵守权限。

- [ ] **M03：统一模型媒体内容块与能力声明。** 扩展文本/图片消息结构，增加 PDF/视频及对应的渠道请求转换。
  - 依赖：M02。
  - 验收：模型支持能力与实际请求编码一致；不支持的类型在请求前被明确拒绝或降级；旧 transcript 和已有图片会话可兼容读取。

- [ ] **M04：Read 支持 PDF 原文及文本提取。** 根据模型能力选择原生 PDF 输入或文本路径。
  - 依赖：M01、M03。
  - 验收：覆盖本地和 SSH 文件读取、体积上限、页数探测及空文件/损坏/加密错误；不把扫描型 PDF 的空文本误报为已完整读取。

- [ ] **M05：PDF 分页渲染。** 提供页码范围、页数上限和图片结果，按预算渲染所选页面。
  - 依赖：M04、M07。
  - 验收：页码越界、取消、超时和组件缺失有明确错误；临时文件可清理；不支持原生 PDF 的视觉模型可按图片路径使用该能力。

- [ ] **M06：Read 与渠道支持视频。** 对支持视频的模型传入受限大小的视频内容；其他模型提供明确的替代路径。
  - 依赖：M03、M07；若采用抽帧则先完成 M01 的组件方案。
  - 验收：有效视频可到达支持它的渠道；类型、大小和预算受控；不支持时明确提示，抽帧摘要不得宣称等同完整视频理解。

- [ ] **M07：统一媒体预算与降级。** 管理单项、单轮和整次请求预算，支持图片缩放、压缩与媒体占位引用。
  - 依赖：M03。
  - 验收：预算按模型与实际发送内容计算；多附件聚合不突破上限；摘要保留附件名称、页码/时间段等引用，不携带原始大媒体；降级对用户可见。

- [ ] **M08：Composer 媒体交互。** 增加 PDF/视频选择、拖入、路径引用、状态和错误展示。
  - 依赖：M04、M05、M06、M07。
  - 验收：上传与 Read 使用一致的能力判断；续聊能定位附件；媒体在不兼容模型间切换时有明确处理，不静默丢弃。

阶段验收：用文本 PDF、扫描 PDF、加密 PDF、大图、多图和视频验证输入、读取、压缩、恢复及模型不支持的完整路径。

## P1/P2：浏览器自动化

主要入口：`src/components/settings/McpSettingsTab.tsx`、`src-tauri/src/native/tools/mcp.rs`、`plugins.rs`、技能发现与桌面窗口管理。

- [ ] **B01：完善现有 Playwright MCP 接入。** 管理已验证版本、安装状态、连接测试与环境诊断。
  - 依赖：M01；复用已有 Playwright 预设。
  - 验收：能完成打开页面、获取 DOM、定位、输入、点击和截图；组件缺失有明确提示；启用、停止、取消与权限生效；不依赖每次启动临时获取 latest。

- [ ] **B02：随包浏览器操作与 GUI 测试技能。** 为当前可用工具提供初始化、观察、操作、验证及失败恢复指导。
  - 依赖：B01。
  - 验收：技能中的工具和能力均实际存在；普通表单和页面测试任务可走通；页面内容作为不可信数据处理，敏感操作继续经过权限入口。

- [ ] **B03：托管浏览器生命周期。** 增加浏览器实例、标签页、用户配置目录、下载和会话结束清理。
  - 依赖：B01。
  - 验收：区分用户已有浏览器与 Agent 托管实例；不会误关用户标签；并发会话不争用同一控制权；浏览器所在主机和文件传输路径明确。

- [ ] **B04：可选 JS 执行内核。** 为需要脚本组合的浏览器能力提供受控 Node/JS 运行环境。
  - 依赖：M01、B03；基础 Browser Use 不以此为前提。
  - 验收：明确每次调用是否使用新内核、状态保存位置及导入范围；执行可取消和超时；文件、网络、进程操作不能绕过宿主权限。

- [ ] **B05：桌面内嵌浏览器整合。** 在 Tauri 桌面显示受控页面和操作状态。
  - 依赖：B03；如使用脚本接口则依赖 B04。
  - 验收：先验证各平台 WebView/浏览器后端的可控能力，不能直接假设兼容 Electron 的 CDP；前后台切换、弹窗、上传和关闭行为一致；不支持的平台提供明确替代入口。

## P2：上下文、记忆与工具契约

主要入口：`src-tauri/src/native/agent/compact.rs`、`agent/truncate.rs`、`memory.rs`、`subagents.rs`、`agent/subagent.rs`、`tools/contract.rs`、`tools/catalog.rs`、`tools/permission.rs`。

- [ ] **C01：以 provider usage 校准上下文预算。** 结合最近一次有效用量与新增内容估算触发压缩，并预留输出空间。
  - 依赖：无；媒体预算在 M07 后接入。
  - 验收：用量缺失时有保守估算；切模型、压缩后不会沿用失效基线；输出预留适配窗口大小，不机械固定为 32K。

- [ ] **C02：压缩失败预算与信息保留。** 记录连续失败，区分失败、无收益和成功，完善现有摘要/本地降级链路。
  - 依赖：C01；附件引用依赖 M02。
  - 验收：连续失败不会无限重试；目标、权限约束、未完成工作及工具调用配对保留；媒体采用引用，不重复引入原始载荷。

- [ ] **C03：工具驱动的记忆整理 Agent。** 在现有抽取和 dream 基础上增加可检索、可多轮整理的受限执行流程。
  - 依赖：无。
  - 验收：读写限制在授权范围；有调用、时间及 token 预算；能合并重复和纠正过期事实；失败不会损坏已有索引，不阻塞会话无限等待。

- [ ] **C04：子 Agent 独立持久记忆。** 增加 user/project/local 作用域及档案配置。
  - 依赖：无；可复用 C03 的整理器，但不强制依赖。
  - 验收：作用域路径稳定且隔离；读取和写入权限明确；只读子 Agent 不因开启记忆而自动获得工作区写权限；本地与 SSH 项目的归属不混淆。

- [ ] **C05：工具输出 schema 与模型能力适配。** 在已有 ToolContract 上增加结构化输出、校验和按模型投影的描述/schema。
  - 依赖：无；媒体工具契约在 M03 后对接。
  - 验收：输出不符合契约时明确失败或受控降级；模型只看到当前可用能力；MCP 工具的契约缺失不能被误认为可信或低风险。

- [ ] **C06：精细化 Bash 只读判断。** 用明确的命令、子命令、选项及 shell 语法策略替换不足的启发式判断。
  - 依赖：无。
  - 验收：覆盖 git/gh、重定向、管道、命令替换、复合命令和未知参数；不确定命令保守处理；本地和 SSH 适用同一权限语义，不把分类器当作完整 OS 沙箱。

阶段验收：长会话压缩、模型窗口下调、记忆冲突、只读子 Agent 和含副作用的复杂 Shell 输入均有针对性回归验证。

## P2：扩展分发与 MCP 授权

主要入口：`src-tauri/src/native/plugins.rs`、`mcp_oauth.rs`、`tools/mcp.rs`、设置页及发布打包配置。

- [ ] **E01：插件市场目录与安装。** 在本地目录/Git 安装之外增加市场元数据、查询、详情与归档下载。
  - 依赖：无。
  - 验收：版本与来源可追溯；归档有完整性校验，解包不允许路径逃逸；失败不会覆盖已安装可用版本；本地/Git 安装继续可用。

- [ ] **E02：插件更新、回退和随包种子。** 区分随包版本、用户更新及用户配置。
  - 依赖：E01。
  - 验收：升级保留配置与启停状态；更新失败可回退；不静默覆盖用户修改；离线时可使用已安装版本；npm 来源仅在 M01 方案允许时接入。

- [ ] **E03：随包文档技能及其运行依赖。** 提供 docx/xlsx/pptx 等创建、读取和验证流程。
  - 依赖：M01、E02。
  - 验收：在声明支持的平台实际生成并读取示例文档、表格和演示文稿；具备必要的渲染/检查路径；缺失转换能力明确提示，不能仅复制 SKILL.md 就标记完成。

- [ ] **E04：MCP OAuth 元数据发现与客户端注册。** 支持服务端公布的授权信息，按能力选择 DCR 或预配置客户端。
  - 依赖：无。
  - 验收：兼容现有手填配置、PKCE 和刷新；缺少 discovery/DCR 时可降级；发现地址和重定向经过验证；令牌不进入普通日志。

- [ ] **E05：协调并发 OAuth 授权。** 为同一授权流程增加唯一活动交互、取消、超时和状态同步。
  - 依赖：E04。
  - 验收：多个会话不会反复打开重复授权窗口；一个流程的回调不能误解锁另一个流程；取消和过期后状态可恢复。

阶段验收：验证全新安装、离线启动、插件升级失败、版本回退、配置保留及并发 MCP 授权。

## P3：Dynamic Workflow 独立子项目

需要新增工作流领域模块、工具、持久化和 UI；复用现有子 Agent、权限、模型目录与工具执行器。下列是里程碑，实施前分别拆出具体任务。

- [ ] **W01：工作流定义、校验和创作。** 确定 Rust DSL/结构化图或受控脚本方案，提供 CreateWorkflow、AmendWorkflow 和只读 snippet 评估。
  - 依赖：C05；脚本方案额外依赖 M01。
  - 验收：输入/输出类型和依赖可校验；非法图、越权能力和不允许的代码在执行前被拒绝；若采用脚本，定义导入、执行隔离及数据流约束。

- [ ] **W02：执行器与持久化 journal。** 建立节点状态、执行结果、失败处理、取消与并发预算。
  - 依赖：W01。
  - 验收：程序重启后可识别已提交和未确认的节点；有副作用节点不被盲目重放；日志与状态迁移一致；权限不能被工作流绕过。

- [ ] **W03：运行查询、人工问答与恢复。** 增加 ListWorkflowRuns、GetWorkflowRun、ResolveWorkflowQuestion、ResumeWorkflowRun，以及 actor 的 submit_result/escalate。
  - 依赖：W02。
  - 验收：运行状态、节点结果、等待问题可查看；回答只投递到对应问题；取消与恢复语义明确；子 Agent 的返回值符合定义的 schema。

- [ ] **W04：保存定义与再次运行。** 增加 SaveWorkflow、ListSavedWorkflows 和供工作流选型使用的 ListModels。
  - 依赖：W01、W03。
  - 验收：定义与运行历史分离；保存版本可复现，修订不改变已开始运行的定义；再次运行前重新检查模型与工具能力。

## P3：独立核心、多端与远程 Host

该阶段按产品需要单独排期，不作为前面桌面功能的前置条件。目标是可复用协议与执行核心，不要求逐字兼容 ZCode Protocol V4。

- [ ] **H01：显式回合状态与执行环境接口。** 渐进拆分当前循环，收口文件、执行、网络、模型、MCP 和存储边界。
  - 依赖：无；保持与 R01 的状态模型一致。
  - 验收：关键状态迁移可测试；核心测试无需真实 Tauri 窗口；本机和 SSH 通过明确接口接入，现有工具行为不退化。

- [ ] **H02：贯通执行链路追踪。** 统一 trace、session、turn、message、tool call、attempt 的关联。
  - 依赖：R01；可随 H01 分步接入。
  - 验收：子 Agent、模型重试、工具、权限和 I/O 能关联到原始任务；日志可定位断点且不默认记录凭据和完整敏感载荷。

- [ ] **H03：独立 Agent Host 与客户端协议。** 提供脱离桌面窗口的运行入口、命令响应、事件序号、快照与重连。
  - 依赖：H01、H02、R03。
  - 验收：客户端断开不等于取消任务；重连后状态可还原；重复命令有幂等处理；schema 可验证且有版本兼容策略。

- [ ] **H04：CLI/TUI 客户端。** 复用 Host 协议实现输入、流式输出、审批、提问、计划和恢复续聊。
  - 依赖：H03。
  - 验收：核心编码任务可从终端完成；交互支持键盘；退出客户端和停止任务的语义明确；不在 TUI 中建立另一份权威会话状态。

- [ ] **H05：多客户端控制权及远程 Host。** 增加 owner/lease、Web/手机接入和远程完整 Agent 执行入口。
  - 依赖：H03。
  - 验收：同一会话不会被多个客户端冲突控制；断线与 lease 失效可恢复；鉴权、主机身份和本机/远端路径明确；与现有“本机 Agent 操作 SSH 工作区”能力并存。

## 条件性任务与不作为对齐缺口的项目

- [ ] **O01：搜索性能与策略评估。** 用代表性本地/SSH 仓库测试现有 rg/Rust 搜索，再决定是否增加 ugrep/bfs 或替代搜索入口。
  - 依赖：无。
  - 验收：记录耗时、资源、忽略规则、符号链接边界和跨平台结果；仅在收益明确时新增后端，不以二进制数量判定功能对齐。

- [ ] **O02：通用闲时调度需求评估。** 若需要低成本时间窗口调度，在现有自动化上设计通用策略。
  - 依赖：Q01。
  - 验收：明确时间窗口、延迟、取消、模型选择和费用来源；只有服务方公开支持且授权可用时才接入套餐能力，不把本地定时执行宣称为官方 OffPeak 权益。

以下内容不列入必须实现范围：

- ZCode 官方 Coding Plan、官方账号身份和套餐权益：依赖服务方接口及授权，不能仅靠修改客户端保证对等。
- 当前 ZCode 未注册的 NotebookEdit 等工具：不作为已有功能差距；未来若有 Jupyter 需求另列任务。
- 当前 ZCode 的 CUA 占位包：不作为可直接移植的实现；继续维护 noxcode 现有 Computer 能力。
- 不为对齐删除或弱化 noxcode 的 SSH、LSP、ApplyPatch、worktree、SQLiteQuery、Process/Monitor。

## 每项实现的完成检查

- 完成该任务的验收场景和必要的错误路径测试；恢复、权限、并发、媒体与数据库任务需覆盖相应边界。
- 修改代码后运行 `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` 和 `npm run format:check`。
- 修改 Rust 运行相关测试；修改前端运行相关 Vitest、`npm run lint` 和 `npm run build`。测试范围依改动确定，不将未执行的检查记录为通过。
- 涉及数据库时验证迁移连续性和旧数据升级；涉及依赖时验证 `cargo tree -i aws-lc-rs` 无匹配及打包依赖约束。
- 在声明支持的平台和本地/SSH 场景执行必要的手工验收；平台未验证部分如实记录。
- 更新对应 `docs/native.md`、架构、渠道、SSH 或发布文档，记录验证结果后再勾选任务。
